---
title: CodeQL 원리와 사용법 
date: 2026-5-17 19:50:00 +0900
categories: [블로그/기술문서]
tags: [기술문서, CodeQL, Static Analysis, DB, SQL]
---

## 개요
[**CodeQL**](https://codeql.github.com): Semmle이 개발하고 GitHub이 2019년에 인수한 Semantic Code Analysis Engine

- Semantic Code Analysis Engine(의미론적 코드 분석 엔진): 코드가 실행되는 의미와 의도를 분석하는 기술

**핵심 아이디어**: 코드를 데이터인 것처럼 쿼리하여 취약점을 분석한다. 

**기존 정적 분석과의 차이**

| 구분      | 기존 정적 분석       | CodeQL                            |
| --------- | -------------------- | --------------------------------- |
| 분석 방식 | 패턴 매칭, 규칙 기반 | 시맨틱 분석(의미 분석)            |
| 표현력    | 단순 패턴만 가능     | 복잡한 데이터/제어 흐름 표현 가능 |
| 오탐율    | 상대적으로 높음      | 상대적으로 낮음                   |
| 분석 범위 | 단일 파일/함수       | 프로젝트 전체 흐름 추적 가능      |

**구성 요소**
1. CodeQL Database - 소스 코드를 분석 가능한 형태로 변환한 관계형 DB
2. Query Language - CodeQL 전용 쿼리 언어
3. CodeQL CLI / Extensions - 분석 실행 환경

---

## 동작 원리
**전체 흐름**  
```
소스 코드  
    ↓ ① Extraction (추출)  
CodeQL Database  
    ↓ ② Query Execution (쿼리 실행)  
Raw Results  
    ↓ ③ Post-processing (후처리)  
최종 분석 결과 (경고, 취약점 위치)
```

---

### ① Extraction (추출)   
소스 코드 데이터를 추출하여 DB에 맞게 변환 후 저장하는 단계이다. 

**추출 과정**
```
소스 코드(.java, .py, .js ...)
    ↓
[언어별 Extractor]
    ↓
① AST (추상 구문 트리)       → 코드 문법 구조
② CFG (제어 흐름 그래프)     → 실행 경로
③ DFG (데이터 흐름 그래프)    → 값의 이동 경로
④ Call Graph (호출 그래프)   → 함수 간 관계
    ↓
CodeQL Database (.db)
```

**AST**: 코드를 트리 구조로 분해한 데이터 구조
![image1](/assets/images/CodeQL/1.png){: width="75%"}
[사진 출처: 트랜스파일러 톺아보기 🔎](https://velog.io/@woogur29/트랜스파일러의-역할과-기능)  

**제어 흐름**: 코드 실행 경로에 따른 조건 분석

**데이터 흐름**: 데이터가 어디에서 어디로 이동하는지 추적

**호출 그래프**: 함수 간 호출 관계를 추적 및 분석


---

### ② Query Execution (쿼리 실행)
준비된 DB에서 쿼리를 실행하여 취약점을 찾는 단계이다. 
- QL은 SQL과 유사하지만 코드 의미를 표현할 수 있다. 

---

### ③ Post-processing (후처리) 
Raw Results를 사람이 읽기 좋은 형태로 변환하고 의미를 해석한다. 
- 중복 제거
- 심각도 (Severity) 분류
- 위치 정보 매핑
- 취약점 경로 시각화 

## 한계점
### 오탐(FP)/미탐(FN) 발생 가능성
**Source/Sink 모델**: Source - 오염된 데이터가 시작되는 지점, Sink - 오염된 데이터가 도착하면 위험한 지점

**오탐(False Positive)**: 실제로 안전한 코드를 위험하다고 판단하는 오류이다. Source와 Sink 사이에 검증 로직(Sanitizer)가 존재하는지를 통해 코드가 안전한지 판단하는데, CodeQL이 모르는 검증 로직을 활용하면 오탐하게 된다.

**미탐(False Negative)**: Source에서 Sink까지의 흐름을 추적하다 중간에 검증 로직이 등장하면 추적을 중단하고 안전하다고 판단하기 때문에 그 뒤에 존재하는 취약점을 발견하지 못하는 미탐 문제가 발생할 수 있다. 항상 코드를 끝까지 추적하게 되면 오탐이 너무 많이 발생하여 검증 로직이 발견되면 추적을 중단할 수밖에 없다. 

### 모델링 의존성과 제로 데이 취약점 발견 어려움
CodeQL은 라이브러리 모델, API 모델, Source/Sink 모델을 기반으로 분석한다. 이 때 알고 있는 함수와 프레임워크가 부족하면 앞서 설명한 오탐/미탐과 같은 문제가 발생하기 때문에 Github 측에서 공개된 프레임워크와 라이브러리에 대한 지속적인 업데이트가 필요하고, 사용자가 커스텀하여 사용하는 함수의 경우 모델링 과정에서 추가로 정의해줘야 한다. 이는 제로 데이 취약점을 발견하기 어렵다는 뜻이기도 하다. 

---

## 실습
### 1. VS Code Extension 설치
VS Code의 Extension 탭에서 **CodeQL** 설치
![image2](/assets/images/CodeQL/2.png)


---

### 2. CodeQL CLI 설치
`brew install codeql` 명령어로 설치 (MacOS 기준)
`codeql --version` 로 설치 확인 (CodeQL command-line toolchain release 2.x.x 출력되면 성공)
![image3](/assets/images/CodeQL/3.png)

---

### 3. 예제 취약점 코드 작성
**디렉토리 구조 생성**  
`mkdir codeql-practice && cd codeql-practice` - 연습용 디렉토리 생성 및 이동  
`mkdir -p src/main/java/com/example` - 자바 실행 구조 생성

**코드 작성**
의도된 취약점이 존재하는 코드 파일 작성
![image4](/assets/images/CodeQL/4.png)

---

### 4. CodeQL DB 생성
프로젝트 경로에서 명령어 실행
``` bash
codeql database create ./codeql-db \ # DB 저장 경로
    --language=java \ # 분석 언어 지정
    --source-root=./src # 소스코드 루트 경로
    --build-mode=none # 빌드 없이 소스코드 만으로 DB생성
```
Successfully created database at /Users/jha/codeql-practice/codeql-db. 문구로 DB생성 성공 확인<br>
<br>

VS Code에서 DB 추가
![image5](/assets/images/CodeQL/5.png)

---

### 5. QL 쿼리 작성 & 실행
**쿼리 작성 경로 및 파일 생성**  
`mkdir queries`  
`touch queries/find-sqli.ql`

**QL 코드 작성**
```
/**
 * @name SQL Injection + XSS 탐지
 * @description 사용자 입력이 SQL 쿼리 또는 HTML 응답으로 흘러가는 경로 탐지
 * @kind path-problem
 * @severity error
 * @id java/sqli-xss-custom
 */

import java
import semmle.code.java.dataflow.TaintTracking
import semmle.code.java.dataflow.FlowSources

private module SqlInjConfig implements DataFlow::ConfigSig {
    predicate isSource(DataFlow::Node source) {
        source instanceof RemoteFlowSource
    }

    predicate isSink(DataFlow::Node sink) {
        // SQL Injection Sink: java.sql 의 execute 계열 메서드
        exists(MethodCall mc |
            mc.getMethod().getName().matches("%execute%") and
            mc.getMethod().getDeclaringType().hasQualifiedName("java.sql", _) and
            sink.asExpr() = mc.getAnArgument()
        )
        or
        // XSS Sink: HttpServletResponse.getWriter().print/println/write 계열 메서드
        exists(MethodCall mc |
            mc.getMethod().getName().matches("%print%") and
            mc.getMethod().getDeclaringType().hasQualifiedName("java.io", "PrintWriter") and
            sink.asExpr() = mc.getAnArgument()
        )
    }
}

module VulnFlow = TaintTracking::Global<SqlInjConfig>;
import VulnFlow::PathGraph

from VulnFlow::PathNode source, VulnFlow::PathNode sink
where VulnFlow::flowPath(source, sink)
select sink.getNode(), source, sink,
    "취약점 발견: 사용자 입력이 검증 없이 위험한 지점으로 전달됨"
```
쿼리 파일을 우클릭하여 "CodeQL: Run Query on Selected Database"로 실행

---

### 6. 결과 확인
![image6](/assets/images/CodeQL/6.png)
의도한 SQLi와 XSS 취약점 모두 분석에 성공했다. 

---

#### SQL Injection
13번 줄에서 받은 사용자 입력이 16번 줄로 검증 없이 전달되었다.  

아래 사진과 같이 취약점 발생 위치를 코드에서 확인할 수 있다. 
![image7](/assets/images/CodeQL/7.png)

---

#### XSS
24번 줄에서 받은 사용자 입력이 25번 줄 HTML 응답에 그대로 출력된다.  
![image8](/assets/images/CodeQL/8.png)

안전한 코드에서는 어떠한 내용도 검출되지 않았다. 

---

## 소감
&nbsp; "코드를 데이터베이스에 쿼리로 보낸다"는 발상이 굉장히 신선하게 다가왔다. 데이터 흐름, 제어 흐름, 호출 그래프를 통해 보다 종합적으로 취약점을 분석하는 점이 실용적이라고 느껴졌다. 또한 SQL 언어와 유사한 점이 많아 익숙하게 느껴지기도 했다. 이러한 도구를 잘 활용하는 것이 굉장히 중요하리라 생각되어 앞으로 더 깊게 공부해보고 싶은 생각이 들었다. 